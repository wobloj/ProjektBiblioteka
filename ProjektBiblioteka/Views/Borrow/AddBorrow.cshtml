@model ProjektBiblioteka.Models.Borrow.BorrowViewModel
@{
    ViewData["Title"] = "Dodaj nowe wypożyczenie";
}

<form method="post" asp-controller="Borrow" asp-action="AddBorrow">
    <div class="form-group">
        <label for="BookId">Książka</label>
        <select asp-for="BookId" class="form-select" asp-items="@(new SelectList(Model.BookList, "Id", "Title"))">
            <option>Wybierz ksiazke</option>
        </select>
        <span asp-validation-for="Book" class="text-danger"></span>
    </div>
    <div class="form-group">
        <label for="PersonId">Osoba</label>
        <select asp-for="PersonId" class="form-select" asp-items="@(new SelectList(Model.PersonList, "Id", "FirstName"))">
            <option>Wybierz osobe</option>
        </select>
        <span asp-validation-for="Person" class="text-danger"></span>
    </div>

    <div class="text-center m-5">
        <a asp-action="Index" class="btn btn-outline-danger"><i class="fa-solid fa-arrow-left me-1"></i>Cofnij</a>
        <button type="submit" class="btn btn-success"><i class="fa-solid fa-plus me-1"></i>Wypożycz</button>
    </div>

    <div class="border rounded text-center m-5 p-4">
        <p class="h3">Nie ma cie na liście?</p>
        <a class="btn btn-success p-3" asp-controller="People" asp-action="AddPerson"><i class="fa-solid fa-plus me-1"></i>Dodaj nową osobe</a>
    </div>
</form>
